import React from 'react';

export default function DotsMenu(props) {
  return (
    <div
      className="dots__wrap"
      // onClick={props.clickHandler(props.triggerId, props.module)}
    >
      <div className="dots__menu"> </div>
    </div>
  );
}
